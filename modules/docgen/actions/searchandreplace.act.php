<?

//-------------------------------------------------------------------------------------------------
//	search for and replace a string in awareNet source code
//-------------------------------------------------------------------------------------------------

	//---------------------------------------------------------------------------------------------
	//	authorization
	//---------------------------------------------------------------------------------------------
	if ('admin' != $user->role) { $page->do403(); }

	//---------------------------------------------------------------------------------------------
	//	check for any post variables
	//---------------------------------------------------------------------------------------------
	$action = ''; $find = ''; $replace = '';

	if (array_key_exists('action', $_POST) == true) { $action = $_POST['action']; }
	if (array_key_exists('find', $_POST) == true) { $find = $_POST['find']; }
	if (array_key_exists('replace', $_POST) == true) { $replace = $_POST['replace']; }

	//---------------------------------------------------------------------------------------------
	//	print a simple form
	//---------------------------------------------------------------------------------------------

	$html = "<html><body>
	<form name='findReplace'>
	<input type='hidden' name='action' value='findReplace' />
	<b>Find:</b>
	<input type='
	<b>Replace:</b>

	</form>
	</body></html>";

?>
